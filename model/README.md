# Model artifacts

`class_labels.json` is the explicit class-index mapping used by inference.

The supplied ML ZIP did not contain the trained `crop_disease_model.h5`. Add that file here for the local/root compatibility path, and add the same trained weights under `ai_service/model/` for the AI service.
